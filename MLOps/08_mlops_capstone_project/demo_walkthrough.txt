PRE-RECORDING SETUP
□ conda activate 22971-mlflow
□ ./reset.sh
□ cd MLOps/08_mlops_capstone_project
□ Open browser to http://localhost:5001 (don't load yet)

============================================================

PART 1 — CODE WALKTHROUGH (~2 min)
SHOW FILES:
- capstone_flow.py (scroll through 9 @step methods)
- capstone_lib.py (highlight key functions)

============================================================

PART 2 — RUN 1: BASELINE (~2 min)
TERMINAL 1:
mlflow server --workers 1 --port 5001 --backend-store-uri sqlite:///mlflow_tracking/mlflow.db --default-artifact-root mlflow_tracking/mlruns

TERMINAL 2:
python capstone_flow.py run --reference-path TLC_data/green_tripdata_2020-01.parquet --batch-path TLC_data/green_tripdata_2020-01.parquet
□ Review interesting logs in the terminal

BROWSER (http://localhost:5001):
□ Open experiment "08_capstone_green_taxi"
□ Review tags in each run: integrity_gate, feature_engineering, bootstrap_train, model_gate
□ Open decision.json artifact in each run and show action="batch_accepted", action="no_retrain"
□ INFERENCE DEMO: Open model_gate run → Artifacts → show predictions.parquet (batch inference on new data)
□ Models → green_taxi_tip_model → Version 1 with @champion alias
□ Say: "Run 1 shows a clean baseline. The batch performs within acceptable tol compared to the ref. No retrain, no promotion"

============================================================

PART 3 — RUN 2: RETRAIN + PROMOTION (~2 min)

TERMINAL:
python capstone_flow.py run --reference-path TLC_data/green_tripdata_2020-01.parquet --batch-path TLC_data/green_tripdata_2020-04.parquet
□ Review interesting logs in the terminal

BROWSER:
□ Refresh experiment
□ Show model_gate run (check rmse_increase_pct > 5%, retrain_recommended=true)
□ Show retrain run (candidate metrics, predictions.parquet)
□ INFERENCE DEMO: Open predictions.parquet artifact → show columns: features + prediction + actual
□ Show promotion_gate run (decision.json with P1-P4, action="promote")
□ Models → green_taxi_tip_model → Version 2 with @champion alias (Version 1 = previous_champion)

============================================================

PART 4 — RUN 3: FAILURE + RESUME (~2 min)

STEP 1 — INJECT FAILURE:
□ Add at capstone_flow.py:line 326 (retrain method): raise RuntimeError("Simulated failure for demo")
□ Show nothing new in the UI

TERMINAL:
python capstone_flow.py run --reference-path TLC_data/green_tripdata_2020-01.parquet --batch-path TLC_data/green_tripdata_2020-06.parquet
(Watch it fail at retrain step)

STEP 2 — FIX:
□ Remove the raise RuntimeError(...) line

TERMINAL:
python capstone_flow.py resume retrain
(Watch it resume from retrain, skip earlier steps)

BROWSER:
□ Refresh experiment
□ Show new retrain and promotion_gate runs
□ Verify no duplicate integrity_gate/feature_engineering runs

============================================================

PART 5 — WRAP-UP (~30 sec)

SUMMARY:
✓ Run 1: Baseline - January batch - no retrain (showed predictions.parquet in model_gate)
✓ Run 2: Retrain + promotion - April batch - degradation detected (showed predictions.parquet in retrain)
✓ Run 3: Failure recovery - June batch - showing Metaflow checkpointing
✓ All decisions logged with decision.json and MLflow tags
✓ Model Registry tracks champion lineage
✓ **Inference demo completed:** batch predictions logged as parquet artifacts with actual vs predicted values

NOTE:
In a production scenario, the next step would be redeploying the serving endpoint with the newly promoted champion.
This could be automated via MLflow Model Serving or a separate CD pipeline.